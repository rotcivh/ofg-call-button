# OFG Call Button

OFG Call Button is a lightweight WordPress plugin that adds a configurable floating click-to-call button with per-page phone overrides, responsive display controls, Persian support, and optional local click statistics.
